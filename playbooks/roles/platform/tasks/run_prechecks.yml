  ---
  - name: Run prechecks from the bin file
    command: "{{ bundles_dir }}/{{ bin_file }} --prechecks-only"
    register: res
    #failed_when: res.rc != 0
    failed_when: false

  - debug:
      msg: "{{ res.stdout }}"
